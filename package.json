{
  "dependencies": {
    "faker": "^6.6.6",
    "mongoose": "^8.2.0",
    "nodemon": "^3.1.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:api": "cd server && nodemon server.ts"
  },
  "devDependencies": {
    "@types/node": "^20.11.20",
    "ts-node": "^10.9.2"
  }
}
